/* Minimal <curses.h>. The programs here include it only because ncurses'
 * <term.h> must be preceded by it; they use nothing from the curses API
 * itself. The termcap entry points are declared in <term.h>. */
#ifndef CURSES_H
#define CURSES_H
#endif
