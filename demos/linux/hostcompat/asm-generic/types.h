/* uapi asm-generic/types.h: the fixed-width set the host tools compile
   against. macOS ships no Linux uapi tree. */
#ifndef _HOSTCOMPAT_ASM_GENERIC_TYPES_H
#define _HOSTCOMPAT_ASM_GENERIC_TYPES_H

#include <asm-generic/int-ll64.h>

#endif
