/* uapi asm/byteorder.h: the host tools build for little-endian targets
   only, and reach the byte-swapping helpers through this name. */
#ifndef _HOSTCOMPAT_ASM_BYTEORDER_H
#define _HOSTCOMPAT_ASM_BYTEORDER_H

#include <endian.h>

#endif
