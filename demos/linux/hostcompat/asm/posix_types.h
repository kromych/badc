/* uapi asm/posix_types.h: every target the host tools build for takes
   the generic definitions. */
#ifndef _HOSTCOMPAT_ASM_POSIX_TYPES_H
#define _HOSTCOMPAT_ASM_POSIX_TYPES_H

#include <asm-generic/posix_types.h>

#endif
