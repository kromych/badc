/* uapi asm/types.h: every target the host tools build for takes the
   generic definitions. */
#ifndef _HOSTCOMPAT_ASM_TYPES_H
#define _HOSTCOMPAT_ASM_TYPES_H

#include <asm-generic/types.h>

#endif
