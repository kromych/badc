/* Demo-local <dbt.h> stub for RGFW's minigamepad backend. RGFW only handles
 * the WM_DEVICECHANGE window message (defined in the win32 message surface)
 * and reads no device-broadcast structures, so this header is empty. */
#ifndef RGFW_DEMO_DBT_H
#define RGFW_DEMO_DBT_H
#endif /* RGFW_DEMO_DBT_H */
