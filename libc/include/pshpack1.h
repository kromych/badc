// pshpack1.h -- push 1-byte struct packing (Win32 SDK alignment
// pragma). Paired with <poppack.h>. No include guard: each include
// pushes another level, matching the SDK header.
#pragma pack(push, 1)
