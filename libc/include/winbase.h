// winbase.h -- in the SDK this is a <windows.h> sub-header, never
// included standalone; badc keeps the kernel32 surface in <windows.h>
// itself, so this name resolves to it.

#pragma once

#include <windows.h>
